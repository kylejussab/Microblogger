<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/global.css">
    <link rel="stylesheet" type="text/css" href="/css/edit.css">
    <title>Microblogger: Edit Draft</title>
</head>

<body>
    <nav>
        <p class="microblogger">Microblogger: Edit Page</p>
    </nav>

    <!-- Back button -->
    <a href="/author"><img src="/assets/arrow.png" class="back-button"></a>

    <h2>Edit Article</h2>

    <div class="error-container">
        <% if(error){ %>
            <p class="error"><%= error %></p>
        <% } %>
    </div>

    <div class="content-container">
        <form method="POST" action="/author/edit/update/<%= draft.id %>">
            <div class="title-subtitle">
                <div class="input-container">
                    <label for="title">Title</label>
                    <input type="text" id="title" name="title" value="<%= draft.title %>" />
                </div>
        
                <div class="input-container">
                    <label for="subtitle">Subtitle</label>
                    <input type="text" id="subtitle" name="subtitle" value="<%= draft.subtitle %>" />
                </div>
            </div>
            
            <div class="body-container">
                <label for="body">Text</label>
                <div class="input-container article-body">
                    <textarea id="body" name="body"><%= draft.body %></textarea>
                </div>
            </div>
            <button type="submit">Update</button>
        </form>

        <div class="additional-information">
            <p>Created</p>
            <p class="additional-text"><%= draft.create_date %></p>
            
            <p>Last Modified</p>
            <p class="additional-text"><%= draft.last_modified %></p>

            <p>Status</p> 
            <p class="additional-text">Draft</p>
        </div>
    </div>

    <footer>
        <p>&copy; 2023 <%= blog.author %>. All rights reserved.</p>
        <p>Terms of Service | Privacy Policy | Contact Us</p>
    </footer>
</body>
</html>